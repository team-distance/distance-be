/*
package io.festival.distance;

import io.festival.distance.domain.member.usecase.MemberUseCase;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DistanceApplicationTests {
    @Autowired
    private MemberUseCase memberUseCase;

	@Test
	void contextLoads() {
       */
/* String telNum ="01063014092";
        String password = "123456";
        String gender = "남자";
        String mbti = "ISFJ";
        String school = "순천향대학교";
        String department = "컴퓨터소프트웨어공학과";
        String college = "SW융합대학";
        String memberCharacter = "호랑이";
        List<MemberTagDto> memberTagDto= Arrays.asList(MemberTagDto.builder()
            .tag("잠자기").build(), MemberTagDto.builder()
            .tag("놀기").build());
        List<MemberHobbyDto> memberHobbyDto = Arrays.asList(MemberHobbyDto.builder()
            .hobby("드라이브").build(), MemberHobbyDto.builder()
            .hobby("drive").build());
        MemberSignDto memberSignDto =new MemberSignDto(
            telNum,
            password,
            mbti,
            gender,
            school,
            department,
            college,
            memberCharacter,
            memberTagDto,
            memberHobbyDto
            );

        Long aLong = memberUseCase.execute(memberSignDto);
        Assertions.assertThat(11L).isEqualTo(aLong);*//*

	}
}*/
